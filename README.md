1. dont runn tests locally.. run tests directly in CI pipeline.

2. husky tool for git hooks

3. docker-compose -- environment consistency in local and CI pipelione

4) multistage dockerfile

5)Builder stage → kitchen (prepare food)
Runner stage → plate (serve only what’s needed)

6. different jobs in ci.yml

7. rate limitting

8. child process + redis/bullMQ (caching)

9) app.js and index.js seperate.
